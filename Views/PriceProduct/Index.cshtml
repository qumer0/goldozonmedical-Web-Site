<div class="container">
    <div class="row">
        @foreach (var product in Model)
        {
            <div class="col-md-4 d-flex justify-content-center">
                <div class="product-card">
                    <img src="@Url.Content(product.ImageUrl)" alt="@product.Name" class="product-image" />
                    <div class="product-info">
                        <h4 class="product-name">@product.Name</h4>
                        <p class="product-price">Eski Fiyat: <span class="original-price">@product.Price ₺</span></p>
                        <p class="discounted-price">İndirimli Fiyat: <span>@product.DiscountedPrice ₺</span></p>
                        <a href="@Url.Action("AddToCart", "Basket", new { id = product.Id })" class="btn btn-secondary add-to-cart-btn">Sepete Ekle</a>
                        <a href="@Url.Action("Buy", "Basket", new { id = product.Id })" class="btn btn-primary buy-btn">Satın Al</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
